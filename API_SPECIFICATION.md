# API Спецификация

## Базовая информация

**Base URL:** `http://localhost:3000`

**Content-Type:** `application/json`

**Authentication:** JWT Token в заголовке `Authorization: Bearer <token>`

---

## 📋 Оглавление

1. [Аутентификация](#аутентификация)
2. [Профиль пользователя](#профиль-пользователя)
3. [Товары (Items)](#товары-items)
4. [Заказы (Orders)](#заказы-orders)
5. [Управление пользователями (Admin)](#управление-пользователями-admin)
6. [Коды ответов](#коды-ответов)
7. [Обработка ошибок](#обработка-ошибок)

**Всего endpoints: 18**

---

## Аутентификация

### 1. Регистрация пользователя

**Endpoint:** `POST /users/sign_up`

**Описание:** Создание нового аккаунта пользователя

**Заголовки:**
```
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "user": {
    "first_name": "John",
    "last_name": "Doe",
    "email": "john.doe@example.com",
    "password": "password123",
    "password_confirmation": "password123"
  }
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| first_name | string | Да | Имя (2-50 символов) |
| last_name | string | Да | Фамилия (2-50 символов) |
| email | string | Да | Email (уникальный) |
| password | string | Да | Пароль (минимум 6 символов) |
| password_confirmation | string | Да | Подтверждение пароля |

**Успешный ответ:** `200 OK`
```json
{
  "status": {
    "code": 200,
    "message": "Signed up successfully."
  },
  "data": {
    "id": 1,
    "email": "john.doe@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "role": "user",
    "full_name": "John Doe",
    "created_at": "2024-02-12T10:30:00.000Z"
  }
}
```

**Заголовки ответа:**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```
> JWT токен для последующих запросов

**Ошибки:**
- `422 Unprocessable Entity` - Ошибка валидации
```json
{
  "status": {
    "message": "User couldn't be created successfully. Email has already been taken"
  }
}
```

---

### 2. Вход в систему

**Endpoint:** `POST /users/sign_in`

**Описание:** Аутентификация существующего пользователя

**Заголовки:**
```
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "user": {
    "email": "john.doe@example.com",
    "password": "password123"
  }
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| email | string | Да | Email пользователя |
| password | string | Да | Пароль |

**Успешный ответ:** `200 OK`
```json
{
  "status": {
    "code": 200,
    "message": "Logged in successfully."
  },
  "data": {
    "id": 1,
    "email": "john.doe@example.com",
    "first_name": "John",
    "last_name": "Doe",
    "role": "user",
    "full_name": "John Doe",
    "created_at": "2024-02-12T10:30:00.000Z"
  }
}
```

**Заголовки ответа:**
```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

**Ошибки:**
- `401 Unauthorized` - Неверные учетные данные

---

### 3. Выход из системы

**Endpoint:** `DELETE /users/sign_out`

**Описание:** Завершение сессии пользователя

**Заголовки:**
```
Authorization: Bearer <token>
```

**Успешный ответ:** `200 OK`
```json
{
  "status": 200,
  "message": "Logged out successfully."
}
```

**Ошибки:**
- `401 Unauthorized` - Активная сессия не найдена

---

## Профиль пользователя

### 4. Получить профиль

**Endpoint:** `GET /api/v1/profile`

**Описание:** Получение информации о текущем пользователе

**Заголовки:**
```
Authorization: Bearer <token>
```

**Успешный ответ:** `200 OK`
```json
{
  "id": 1,
  "email": "john.doe@example.com",
  "first_name": "John",
  "last_name": "Doe",
  "role": "user",
  "full_name": "John Doe",
  "created_at": "2024-02-12T10:30:00.000Z"
}
```

**Ошибки:**
- `401 Unauthorized` - Требуется аутентификация

---

### 5. Обновить профиль

**Endpoint:** `PUT /api/v1/profile` или `PATCH /api/v1/profile`

**Описание:** Обновление данных текущего пользователя

**Заголовки:**
```
Authorization: Bearer <token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "user": {
    "first_name": "Jane",
    "last_name": "Smith",
    "email": "jane.smith@example.com",
    "password": "newpassword123",
    "password_confirmation": "newpassword123",
    "current_password": "oldpassword123"
  }
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| first_name | string | Нет | Новое имя |
| last_name | string | Нет | Новая фамилия |
| email | string | Нет | Новый email |
| password | string | Нет* | Новый пароль |
| password_confirmation | string | Нет* | Подтверждение пароля |
| current_password | string | Да** | Текущий пароль |

\* Оба поля обязательны при смене пароля  
\*\* Обязательно при смене email или пароля

**Успешный ответ:** `200 OK`
```json
{
  "message": "Profile updated successfully",
  "user": {
    "id": 1,
    "email": "jane.smith@example.com",
    "first_name": "Jane",
    "last_name": "Smith",
    "role": "user",
    "full_name": "Jane Smith",
    "created_at": "2024-02-12T10:30:00.000Z"
  }
}
```

**Ошибки:**
- `422 Unprocessable Entity` - Ошибка валидации
- `401 Unauthorized` - Неверный current_password

---

## Товары (Items)

### 6. Получить список товаров

**Endpoint:** `GET /api/v1/items`

**Описание:** Получение списка всех товаров с поддержкой поиска и пагинации

**Заголовки:**
```
Authorization: Bearer <token>
```

**Query параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| search | string | Нет | Поиск по названию (ILIKE) |
| page | integer | Нет | Номер страницы (по умолчанию: 1) |
| per_page | integer | Нет | Элементов на странице (по умолчанию: 20) |

**Примеры запросов:**
```
GET /api/v1/items
GET /api/v1/items?search=laptop
GET /api/v1/items?page=2&per_page=10
GET /api/v1/items?search=phone&page=1&per_page=5
```

**Успешный ответ:** `200 OK`
```json
{
  "items": [
    {
      "id": 1,
      "name": "Laptop Dell XPS 13",
      "description": "Powerful ultrabook with 16GB RAM and 512GB SSD",
      "price": "1299.99",
      "formatted_price": "1299.99",
      "created_at": "2024-02-12T10:00:00.000Z",
      "updated_at": "2024-02-12T10:00:00.000Z"
    },
    {
      "id": 2,
      "name": "iPhone 15 Pro",
      "description": "Latest Apple smartphone with A17 chip",
      "price": "999.99",
      "formatted_price": "999.99",
      "created_at": "2024-02-12T10:00:00.000Z",
      "updated_at": "2024-02-12T10:00:00.000Z"
    }
  ],
  "meta": {
    "current_page": 1,
    "next_page": 2,
    "prev_page": null,
    "total_pages": 5,
    "total_count": 100
  }
}
```

---

### 7. Получить товар по ID

**Endpoint:** `GET /api/v1/items/:id`

**Описание:** Получение детальной информации о товаре

**Заголовки:**
```
Authorization: Bearer <token>
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID товара |

**Пример:** `GET /api/v1/items/1`

**Успешный ответ:** `200 OK`
```json
{
  "id": 1,
  "name": "Laptop Dell XPS 13",
  "description": "Powerful ultrabook with 16GB RAM and 512GB SSD",
  "price": "1299.99",
  "formatted_price": "1299.99",
  "created_at": "2024-02-12T10:00:00.000Z",
  "updated_at": "2024-02-12T10:00:00.000Z"
}
```

**Ошибки:**
- `404 Not Found` - Товар не найден

---

### 8. Создать товар (Admin)

**Endpoint:** `POST /api/v1/items`

**Описание:** Создание нового товара (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "item": {
    "name": "MacBook Pro 16",
    "description": "Professional laptop with M3 Max chip",
    "price": 2499.99
  }
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| name | string | Да | Название (2-200 символов) |
| description | text | Нет | Описание (до 2000 символов) |
| price | decimal | Да | Цена (больше 0) |

**Успешный ответ:** `201 Created`
```json
{
  "message": "Item created successfully",
  "item": {
    "id": 11,
    "name": "MacBook Pro 16",
    "description": "Professional laptop with M3 Max chip",
    "price": "2499.99",
    "formatted_price": "2499.99",
    "created_at": "2024-02-12T15:30:00.000Z",
    "updated_at": "2024-02-12T15:30:00.000Z"
  }
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `422 Unprocessable Entity` - Ошибка валидации

---

### 9. Обновить товар (Admin)

**Endpoint:** `PUT /api/v1/items/:id` или `PATCH /api/v1/items/:id`

**Описание:** Обновление товара (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID товара |

**Тело запроса:**
```json
{
  "item": {
    "name": "MacBook Pro 16 (Updated)",
    "price": 2399.99
  }
}
```

**Успешный ответ:** `200 OK`
```json
{
  "message": "Item updated successfully",
  "item": {
    "id": 11,
    "name": "MacBook Pro 16 (Updated)",
    "description": "Professional laptop with M3 Max chip",
    "price": "2399.99",
    "formatted_price": "2399.99",
    "created_at": "2024-02-12T15:30:00.000Z",
    "updated_at": "2024-02-12T16:00:00.000Z"
  }
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `404 Not Found` - Товар не найден
- `422 Unprocessable Entity` - Ошибка валидации

---

### 10. Удалить товар (Admin)

**Endpoint:** `DELETE /api/v1/items/:id`

**Описание:** Удаление товара (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID товара |

**Пример:** `DELETE /api/v1/items/11`

**Успешный ответ:** `200 OK`
```json
{
  "message": "Item deleted successfully"
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `404 Not Found` - Товар не найден

---

## Заказы (Orders)

### 11. Получить список заказов

**Endpoint:** `GET /api/v1/orders`

**Описание:** Получение списка заказов текущего пользователя

**Заголовки:**
```
Authorization: Bearer <token>
```

**Query параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| page | integer | Нет | Номер страницы (по умолчанию: 1) |
| per_page | integer | Нет | Элементов на странице (по умолчанию: 20) |

**Примеры:** 
```
GET /api/v1/orders
GET /api/v1/orders?page=2&per_page=10
```

**Успешный ответ:** `200 OK`
```json
{
  "orders": [
    {
      "id": 1,
      "amount": "2699.97",
      "formatted_amount": "2699.97",
      "items_count": 3,
      "status": "completed",
      "created_at": "2024-02-12T10:30:00.000Z",
      "updated_at": "2024-02-12T10:30:00.000Z"
    },
    {
      "id": 2,
      "amount": "1299.99",
      "formatted_amount": "1299.99",
      "items_count": 1,
      "status": "processing",
      "created_at": "2024-02-11T14:20:00.000Z",
      "updated_at": "2024-02-11T14:20:00.000Z"
    }
  ],
  "meta": {
    "current_page": 1,
    "next_page": null,
    "prev_page": null,
    "total_pages": 1,
    "total_count": 2
  }
}
```

---

### 12. Получить детали заказа

**Endpoint:** `GET /api/v1/orders/:id`

**Описание:** Получение детальной информации о заказе со всеми товарами

**Заголовки:**
```
Authorization: Bearer <token>
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID заказа |

**Пример:** `GET /api/v1/orders/1`

**Успешный ответ:** `200 OK`
```json
{
  "data": {
    "id": "1",
    "type": "order",
    "attributes": {
      "id": 1,
      "amount": "2699.97",
      "formatted_amount": "2699.97",
      "items_count": 3,
      "status": "completed",
      "created_at": "2024-02-12T10:30:00.000Z",
      "updated_at": "2024-02-12T10:30:00.000Z"
    },
    "relationships": {
      "user": {
        "data": {
          "id": "2",
          "type": "user"
        }
      },
      "order_items": {
        "data": [
          {
            "id": "1",
            "type": "order_item"
          },
          {
            "id": "2",
            "type": "order_item"
          }
        ]
      }
    }
  },
  "included": [
    {
      "id": "1",
      "type": "order_item",
      "attributes": {
        "id": 1,
        "quantity": 2,
        "subtotal": 2599.98,
        "formatted_subtotal": "2599.98",
        "created_at": "2024-02-12T10:30:00.000Z"
      },
      "relationships": {
        "item": {
          "data": {
            "id": "1",
            "type": "item"
          }
        }
      }
    },
    {
      "id": "2",
      "type": "order_item",
      "attributes": {
        "id": 2,
        "quantity": 1,
        "subtotal": 99.99,
        "formatted_subtotal": "99.99",
        "created_at": "2024-02-12T10:30:00.000Z"
      },
      "relationships": {
        "item": {
          "data": {
            "id": "2",
            "type": "item"
          }
        }
      }
    },
    {
      "id": "1",
      "type": "item",
      "attributes": {
        "id": 1,
        "name": "Laptop Dell XPS 13",
        "description": "Powerful ultrabook...",
        "price": "1299.99",
        "formatted_price": "1299.99"
      }
    },
    {
      "id": "2",
      "type": "item",
      "attributes": {
        "id": 2,
        "name": "Wireless Mouse",
        "description": "Ergonomic mouse...",
        "price": "99.99",
        "formatted_price": "99.99"
      }
    }
  ]
}
```

**Ошибки:**
- `404 Not Found` - Заказ не найден
- `403 Forbidden` - Заказ принадлежит другому пользователю (если не admin)

---

### 13. Создать заказ

**Endpoint:** `POST /api/v1/orders`

**Описание:** Создание нового заказа из корзины

**Заголовки:**
```
Authorization: Bearer <token>
Content-Type: application/json
```

**Тело запроса:**
```json
{
  "cart_items": [
    {
      "item_id": 1,
      "quantity": 2
    },
    {
      "item_id": 3,
      "quantity": 1
    }
  ]
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| cart_items | array | Да | Массив товаров для заказа |
| cart_items[].item_id | integer | Да | ID товара |
| cart_items[].quantity | integer | Да | Количество (больше 0) |

**Успешный ответ:** `201 Created`
```json
{
  "message": "Order created successfully",
  "order": {
    "data": {
      "id": "3",
      "type": "order",
      "attributes": {
        "id": 3,
        "amount": "2999.97",
        "formatted_amount": "2999.97",
        "items_count": 3,
        "status": "pending",
        "created_at": "2024-02-12T16:00:00.000Z",
        "updated_at": "2024-02-12T16:00:00.000Z"
      },
      "relationships": {
        "order_items": {
          "data": [...]
        }
      }
    },
    "included": [...]
  }
}
```

**Ошибки:**
- `422 Unprocessable Entity` - Ошибка валидации (товар не найден, неверное количество)
- `401 Unauthorized` - Требуется аутентификация

---

### 18. Обновить статус заказа (Admin)

**Endpoint:** `PATCH /api/v1/orders/:id/status`

**Описание:** Обновление статуса заказа (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID заказа |

**Тело запроса:**
```json
{
  "status": "shipped"
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| status | string | Да | Новый статус заказа |

**Допустимые значения status:**
- `pending` - Ожидает обработки
- `processing` - В обработке
- `shipped` - Отправлен
- `completed` - Завершен
- `cancelled` - Отменен

**Lifecycle статусов:**
```
pending → processing → shipped → completed
   └──────────────┴──────────────> cancelled
```

**Успешный ответ:** `200 OK`
```json
{
  "message": "Order status updated successfully",
  "order": {
    "id": 1,
    "status": "shipped",
    "amount": "2699.97",
    "updated_at": "2024-02-12T16:30:00.000Z"
  }
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `404 Not Found` - Заказ не найден
- `422 Unprocessable Entity` - Недопустимый статус или невозможный переход

---

## Управление пользователями (Admin)

### 14. Получить список пользователей (Admin)

**Endpoint:** `GET /api/v1/users`

**Описание:** Получение списка всех пользователей (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
```

**Query параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| page | integer | Нет | Номер страницы (по умолчанию: 1) |
| per_page | integer | Нет | Элементов на странице (по умолчанию: 20) |

**Успешный ответ:** `200 OK`
```json
{
  "users": [
    {
      "id": 1,
      "email": "admin@example.com",
      "first_name": "Admin",
      "last_name": "User",
      "role": "admin",
      "full_name": "Admin User",
      "created_at": "2024-02-10T10:00:00.000Z"
    },
    {
      "id": 2,
      "email": "john.doe@example.com",
      "first_name": "John",
      "last_name": "Doe",
      "role": "user",
      "full_name": "John Doe",
      "created_at": "2024-02-12T10:30:00.000Z"
    }
  ],
  "meta": {
    "current_page": 1,
    "next_page": null,
    "prev_page": null,
    "total_pages": 1,
    "total_count": 2
  }
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора

---

### 15. Получить пользователя по ID (Admin)

**Endpoint:** `GET /api/v1/users/:id`

**Описание:** Получение информации о конкретном пользователе

**Заголовки:**
```
Authorization: Bearer <admin_token>
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID пользователя |

**Успешный ответ:** `200 OK`
```json
{
  "id": 2,
  "email": "john.doe@example.com",
  "first_name": "John",
  "last_name": "Doe",
  "role": "user",
  "full_name": "John Doe",
  "created_at": "2024-02-12T10:30:00.000Z"
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `404 Not Found` - Пользователь не найден

---

### 16. Обновить пользователя (Admin)

**Endpoint:** `PUT /api/v1/users/:id` или `PATCH /api/v1/users/:id`

**Описание:** Обновление данных пользователя (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
Content-Type: application/json
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID пользователя |

**Тело запроса:**
```json
{
  "user": {
    "first_name": "Jane",
    "last_name": "Doe",
    "email": "jane.doe@example.com",
    "role": "admin"
  }
}
```

**Параметры:**
| Параметр | Тип | Обязательный | Описание |
|----------|-----|--------------|----------|
| first_name | string | Нет | Новое имя |
| last_name | string | Нет | Новая фамилия |
| email | string | Нет | Новый email |
| role | string | Нет | Роль ('admin' или 'user') |

**Успешный ответ:** `200 OK`
```json
{
  "message": "User updated successfully",
  "user": {
    "id": 2,
    "email": "jane.doe@example.com",
    "first_name": "Jane",
    "last_name": "Doe",
    "role": "admin",
    "full_name": "Jane Doe",
    "created_at": "2024-02-12T10:30:00.000Z"
  }
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора
- `404 Not Found` - Пользователь не найден
- `422 Unprocessable Entity` - Ошибка валидации

---

### 17. Удалить пользователя (Admin)

**Endpoint:** `DELETE /api/v1/users/:id`

**Описание:** Удаление пользователя (только для администраторов)

**Заголовки:**
```
Authorization: Bearer <admin_token>
```

**URL параметры:**
| Параметр | Тип | Описание |
|----------|-----|----------|
| id | integer | ID пользователя |

**Успешный ответ:** `200 OK`
```json
{
  "message": "User deleted successfully"
}
```

**Ошибки:**
- `403 Forbidden` - Требуются права администратора или попытка удалить самого себя
```json
{
  "error": "You cannot delete yourself"
}
```
- `404 Not Found` - Пользователь не найден

---

## Коды ответов

| Код | Описание |
|-----|----------|
| **200 OK** | Запрос успешно выполнен |
| **201 Created** | Ресурс успешно создан |
| **400 Bad Request** | Неверный формат запроса |
| **401 Unauthorized** | Требуется аутентификация или неверные учетные данные |
| **403 Forbidden** | Доступ запрещен (недостаточно прав) |
| **404 Not Found** | Ресурс не найден |
| **422 Unprocessable Entity** | Ошибка валидации данных |
| **500 Internal Server Error** | Внутренняя ошибка сервера |

---

## Обработка ошибок

### Формат ошибок валидации (422)

```json
{
  "errors": [
    "Email has already been taken",
    "Password is too short (minimum is 6 characters)"
  ]
}
```

### Формат общих ошибок (400, 403, 404)

```json
{
  "error": "Access denied. Admin rights required."
}
```

### Формат ошибок аутентификации (401)

```json
{
  "error": "You need to sign in or sign up before continuing."
}
```

---

## Примеры использования

### Полный workflow создания заказа

#### 1. Регистрация
```bash
curl -X POST http://localhost:3000/users/sign_up \
  -H "Content-Type: application/json" \
  -d '{
    "user": {
      "first_name": "John",
      "last_name": "Doe",
      "email": "john@example.com",
      "password": "password123",
      "password_confirmation": "password123"
    }
  }'
```

#### 2. Получение токена из заголовка ответа
```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...
```

#### 3. Просмотр товаров
```bash
curl -X GET http://localhost:3000/api/v1/items \
  -H "Authorization: Bearer <token>"
```

#### 4. Поиск товара
```bash
curl -X GET "http://localhost:3000/api/v1/items?search=laptop" \
  -H "Authorization: Bearer <token>"
```

#### 5. Создание заказа
```bash
curl -X POST http://localhost:3000/api/v1/orders \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "cart_items": [
      {"item_id": 1, "quantity": 2},
      {"item_id": 3, "quantity": 1}
    ]
  }'
```

#### 6. Просмотр заказов
```bash
curl -X GET http://localhost:3000/api/v1/orders \
  -H "Authorization: Bearer <token>"
```

#### 7. Детали заказа
```bash
curl -X GET http://localhost:3000/api/v1/orders/1 \
  -H "Authorization: Bearer <token>"
```

---

## Health Check

**Endpoint:** `GET /health`

**Описание:** Проверка работоспособности API

**Успешный ответ:** `200 OK`
```
OK
```

Используется для мониторинга и проверки доступности сервера.

---

## Дополнительная информация

### JWT Token
- Время жизни: 1 день
- Хранится в заголовке `Authorization`
- Формат: `Bearer <token>`
- После истечения требуется повторный вход

### Pagination
- Используется gem Kaminari
- По умолчанию 20 элементов на странице
- Максимум 100 элементов на странице

### CORS
- Разрешены все origins (для разработки)
- В продакшене настроить на конкретные домены


---

**Версия API:** 1.1  
**Дата обновления:** 13 февраля 2026
**Backend:** Rails 7.1.0  
**База данных:** PostgreSQL 14+



